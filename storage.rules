rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow authenticated users to read/write files
      // In a production app, we would restrict this to specific paths like /user/{uid}/...
      allow read, write: if request.auth != null;
    }
  }
}
